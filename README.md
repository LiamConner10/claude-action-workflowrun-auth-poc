# Controlled workflow_run payload observer

This repository records non-secret GitHub event fields for a controlled two-account
HackerOne test. It does not run Claude Code, accept untrusted commands, or expose
repository secrets.
